# --- Ядро пайплайна ---
anthropic>=0.49            # анализ хуков и генерация сценариев через Claude
yt-dlp>=2024.1.0           # скачивание видео из TikTok / Shorts / Reels
python-dotenv>=1.0         # загрузка настроек из .env
requests>=2.31

# --- Озвучка (бесплатный TTS от Microsoft Edge) ---
edge-tts>=6.1

# --- Транскрипция хуков (опционально, тяжёлая зависимость) ---
# Можно не ставить — пайплайн отработает и без неё, просто без текста хука.
faster-whisper>=1.0

# --- Загрузка на YouTube (опционально) ---
google-api-python-client>=2.100
google-auth-oauthlib>=1.2

# ВНИМАНИЕ: дополнительно нужны системные бинарники ffmpeg и ffprobe
# (см. README.md, раздел «Что нужно для работы»).
